using Assets.Code.Interfaces;
using UnityEngine;

public class EnemyHitVFX : MonoBehaviour, IVFX
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
